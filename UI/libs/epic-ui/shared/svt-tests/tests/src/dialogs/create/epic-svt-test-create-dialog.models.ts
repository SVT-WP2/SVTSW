export namespace EpicSvtTestCreateDialog {

    /**
     * The dialog always opens empty — a SvtTest is never cloned or edited, so there is nothing to seed it with.
     */
    export type Data = Record<string, never>

}
