export namespace EpicLayoutLightDynamicSection {

    export type SectionName = 'Logo' | 'Header' | 'HeaderAction' | 'SidePanel'
    export const SectionName = {
        Logo: 'Logo' as SectionName,
        Header: 'Header' as SectionName,
        HeaderAction: 'HeaderAction' as SectionName,
        SidePanel: 'SidePanel' as SectionName,
    }
}
