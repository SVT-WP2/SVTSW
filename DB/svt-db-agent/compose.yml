services:
  svt.db-agent:
    image: localhost/svt.db-agent-deploy:latest
    build:
      context: . # The directory containing your Dockerfile
      target: db-agent-deploy
    network_mode: "host"
    volumes:
      - /tmp:/tmp
